.graph-viewer(ng-if="fasdf")
  .distribution-graph

  .info-port
    .coders(ng-if="displayCoders")
      .num {{numCoders}}
      .label CODERS
    .rating(ng-if="!displayCoders")
      .num {{rating}}
      .label RATING
