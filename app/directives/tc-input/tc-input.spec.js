/* jshint -W117, -W030 */
describe.only('Topcoder Input Directive', function() {
  var scope;

  beforeEach(function() {
    bard.appModule('tcUIComponents');
    bard.inject(this, '$compile', '$rootScope');
  });

  bard.verifyNoOutstandingHttpRequests();

  describe('', function() {
    beforeEach(function() {});

    it('', function() {});
  });
});
